general:
  build_dir: game
checkout:
  post:
    - git submodule sync
    - git submodule update --init
dependencies:
  post:
   - npm install -g node-qunit-phantomjs
test:
  override:
    - node-qunit-phantomjs /test-suite/tests.html