general:
    build_dir: game
checkout:
    post:
        - git submodule sync
        - git submodule update --init
dependencies:
    pre:
        - npm install -g karma-cli bower
        - bower install
test:
    override:
        - npm test