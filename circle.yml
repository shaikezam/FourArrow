general:
  build_dir: game
checkout:
  post:
    - git submodule sync
    - git submodule update --init
dependencies:
  post:
    - qunit ./install-packages
test:
  override:
    - qunit ./test-suite/test.html --unit-tests