general:
    build_dir: game
checkout:
    post:
        - git submodule sync
        - git submodule update --init
dependencies:
    pre:
        - npm install
        - npm install -g karma-cli
        - npm eslint
test:
    override:
        - npm test